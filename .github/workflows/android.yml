name: build

# 构建 WikiMind React Native Debug APK
# GitHub Runner 可以访问 Google Maven / dl.google.com，本地沙箱不行。
# push 到任何分支都触发；APK 作为 artifact 上传，可直接下载安装。

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main]

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-wikimind-apk:
    name: WikiMind assembleDebug
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: 'platform-tools platforms;android-34 build-tools;34.0.0 ndk;26.1.10909125'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-rn-${{ runner.os }}-${{ hashFiles('android/**/*.gradle*', 'android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-rn-${{ runner.os }}-

      - name: Install npm dependencies
        run: npm ci

      - name: Verify environment
        run: |
          echo "Node: $(node --version)"
          echo "npm: $(npm --version)"
          echo "Java: $(java -version 2>&1 | head -1)"
          echo "ANDROID_HOME: $ANDROID_HOME"
          ls $ANDROID_HOME/platforms/ || echo "no platforms"
          ls $ANDROID_HOME/build-tools/ || echo "no build-tools"

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Assemble debug APK
        id: build
        run: |
          cd android
          ./gradlew :app:assembleDebug --no-daemon --stacktrace 2>&1 | tee /tmp/build.log
          BUILD_EXIT=${PIPESTATUS[0]}
          if [ $BUILD_EXIT -ne 0 ]; then
            echo ""
            echo "========== BUILD FAILED — KEY ERRORS =========="
            grep -E "FAILED|error:|Error |Exception|Cannot|Could not" /tmp/build.log | grep -v "^Note:" | tail -60
            echo "========== END =========="
          fi
          exit $BUILD_EXIT
        env:
          ANDROID_HOME: ${{ env.ANDROID_HOME }}

      - name: Upload debug APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: wikimind-debug-${{ github.run_number }}
          path: android/app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
          retention-days: 14
